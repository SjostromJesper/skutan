const boatCost = 1000
const beerCost = 80
const drinkCost = 100
const wine = 100
const soda = 50

export const calculateCost = (purchases) => {
    const sum = {}

    for(let category in purchases) {
        console.log(category)
    }
}